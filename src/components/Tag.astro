---
interface Props {
	tag: string;
}

const { tag } = Astro.props;
---

<a
  href={`/tags/${tag}`}
  class="inline-flex items-center px-2 py-1 text-sm text-gray-500 hover:text-gray-900 transition-colors"
>
  <span class="mr-1">#</span>{tag}
</a>
